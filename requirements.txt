# Core radar dependencies
flask>=3.0
flask-cors
requests>=2.31
websocket-client>=1.7

# Phase 4+ — wallet signing on Polymarket / SX Bet (EIP-712).
# Optional in Phase 4 (LocalEnvStore.sign returns None gracefully without
# eth-account installed); REQUIRED before flipping DRY_RUN=0 (Phase 5 gate).
eth-account>=0.11

# Phase 6 — Polygon RPC for USDC balance reads + rebalance transfers.
# Optional. Not needed for dry-run.
# web3>=6.13

# Phase 6 — AWS Secrets Manager backend (production VPS).
# Optional. Only needed when WALLET_BACKEND=aws.
# boto3>=1.34
